# Entity

## 목적
고유 식별자(ID)를 가진 도메인 객체를 정의합니다.

## 특징
- 고유 식별자로 구분됨 (동일한 ID = 동일한 객체)
- 생명주기를 가짐 (생성, 수정, 삭제)
- 비즈니스 규칙과 행동을 포함
- 프레임워크에 독립적 (순수 Python)

## 책임
- 엔티티의 상태 관리
- 엔티티 관련 비즈니스 규칙 검증
- 엔티티의 행동(메서드) 정의